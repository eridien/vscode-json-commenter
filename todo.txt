bugs ...
  typing in box (need doc change support)
  leading spaces in block lost when going to edit

todo ...
  text = text.replaceAll(/"/g, settings.quoteStr); 
  word wrap
  detect and close all edit areas in docs when visible editors change
  dark theme highlight
  set editor.unicodeHighlight.invisibleCharacters

settings ...
  indent: 4,
  marginTop: 2,
  marginBottom: 2,
  padding: 2,
  width: 40,
  quoteStr: "'",
  headerStr: '-',
  footerStr: '-',
  beforeClickPos: false,
  editing highlight color

works in ...
  package.json
  package-lock.json
  .vscode/settings.json
  .vscode/launch.json

doesn't work in ...
  bookmarks.json, comments cleared on reboot
