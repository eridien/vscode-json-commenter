bugs ...
  new box inside another box broken
  doc change event during startedit

todo ...
  detect and close all edit areas in docs when visible editors change
  remove decoration
  put inv chars in comment tags
  dark theme highlight
  set editor.unicodeHighlight.invisibleCharacters
  text = text.replaceAll(/"/g, settings.quoteStr); 

settings ...
  indent: 4,
  marginTop: 2,
  marginBottom: 2,
  padding: 2,
  width: 40,
  quoteStr: "'",
  headerStr: '-',
  footerStr: '-',
  beforeClickPos: false,
  editing highlight color

works in ...
  package.json
  package-lock.json
  .vscode/settings.json
  .vscode/launch.json

doesn't work in ...
  bookmarks.json, comments cleared on reboot

// change all functions to use an object instead for params,   
// the type of that object should be "any", just like drawBox
