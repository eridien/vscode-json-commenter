bugs ...
  before mode and after last file char -> comma wrong (after)
  after mode and after last point -> comma wrong (after)
  after mode and two props on same line, 
    click between left of each prop -> wrong aftertext offset
  before mode and two props on same line, 
    click between right of each prop -> wrong aftertext offset
  before in projects: array -> adds a line below
  