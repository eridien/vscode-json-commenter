bugs ...
  very slow first box building

todo ...
  click here <-> type here
  min width, max width
  text = text.replaceAll(/"/g, settings.quoteStr); 
  word wrap
  detect and close all edit areas in docs when visible editors change
  dark theme highlight
  set editor.unicodeHighlight.invisibleCharacters

settings ...
  indent: 4,
  marginTop: 2,
  marginBottom: 2,
  padding: 2,
  min width: 30,
  max width: 60,
  quoteStr: "'",
  headerStr: '-',
  footerStr: '-',
  beforeClickPos: false,
  editing highlight color

works in ...
  package.json
  package-lock.json
  .vscode/settings.json
  .vscode/launch.json

doesn't work in ...
  bookmarks.json, comments cleared on reboot
